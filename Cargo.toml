[package]
name = "fractals"
version = "0.1.0"
authors = ["Jeremy D. Frens <jfrens@thecitybase.com>"]
edition = "2018"

[features]
# Treat warnings as a build error.
strict = []

[dependencies]
num-complex = "0.4"
image = "0.23"
yaml-rust = "0.4"

[dev-dependencies]
proptest = "0.9.4"
